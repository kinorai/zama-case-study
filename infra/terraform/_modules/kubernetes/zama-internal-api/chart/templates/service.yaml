apiVersion: v1
kind: Service
metadata:
  name: {{ include "zama-internal-api.fullname" . }}
  labels:
    {{- include "zama-internal-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "zama-internal-api.selectorLabels" . | nindent 4 }}
