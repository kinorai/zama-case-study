apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "zama-internal-api.fullname" . }}-test-connection"
  labels:
    {{- include "zama-internal-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "zama-internal-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
